kind: Macros
metadata:
  namespace: ணக்கம்@@@@@@@@@@@@
spec:
  - name: "k8s.AppAndEnvLabels-வணக்கம்-@@@@@@@@@@@@"
    description: This is a macro for adding labels
    parameters:
      application: "வவணக்கம்- @@@@@@@@@@@@"
      environment: "வணக்கம் - @@@@@@@@@@@@"
    processor:
      - type: freemarker
        description: "Adding app and env label to kubernetes வவணக்கம்- @@@@@@@@@@@@ spec"      
        parameters:
          template: |
            { "metadata" : {
              "labels": {
                "application": "${params['application']}",
                "environment": "${params['environment']}",
              }
             }
            }
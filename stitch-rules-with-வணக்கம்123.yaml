kind: Macros
metadata:
  namespace: common-k8s-tasks-வணக்கம்123455
spec:
  - name: "k8s.AppAndEnvLabels-வணக்கம்"
    description: This is a macro for adding labels
    parameters:
      application: "வணக்கம்123455"
      environment: "வணக்கம்123455"
    processor:
      - type: freemarker
        description: "Adding app and env label to kubernetes spec"      
        parameters:
          template: |
            { "metadata" : {
              "labels": {
                "application": "${params['application']}",
                "environment": "${params['environment']}",
              }
             }
            }